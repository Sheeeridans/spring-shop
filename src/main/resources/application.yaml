spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: postgres
    password: 123456
  flyway:
    baseline-on-migrate: true
  main:
    allow-circular-references: true